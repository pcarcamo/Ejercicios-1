using System;

public class Program
{
    public static void Main()
    {
		            float Numero1, Numero2, Numero3, Numero4;
        float val = (float)10;
        float val2 = (float)1598.922;
        float operacion1, operacion2, operacion;

        Console.WriteLine("Ingrese la distancia");
        Numero1 = Convert.ToSingle(Console.ReadLine());
        
        Console.WriteLine("Ingrese la hora");
        Numero2 = Convert.ToSingle(Console.ReadLine());

        Console.WriteLine("Ingrese ls minutos");
       	Numero3 = Convert.ToSingle(Console.ReadLine());

        Console.WriteLine("Ingrese los segundos");
        Numero4 = Convert.ToSingle(Console.ReadLine());


        operacion1 = Numero1 / (Numero4 / val);
        Console.WriteLine("Su velocidad en metros de {0} {1} es {2}  ", Numero1, Numero4, operacion1);

        operacion2 = Numero1 / (Numero2*val2);
        Console.WriteLine("Su velocidad en kilometros de {0} {1} es {2}",  Numero1, Numero2, operacion2);
          

          operacion = Numero1 / (Numero3 * val); 
          Console.WriteLine("Su velocidad en millas de {0} {1} es {2}",  Numero1, Numero3, operacion);
          Console.ReadLine();

    }
}
